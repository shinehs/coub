define(['share'], (share) => {
    let dialog = {
        init() {
            this.addEventListener();
            return this;
        },
        addEventListener() {
           
        }
    };

    return dialog;
});